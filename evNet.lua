function initPop (size, layers, layerSize)
    assert(type(layers) == type(layerSize), "Incorrect parameter types")
    assert(size > 0, "Size must be greater than zero")
    population = {}
    for s in size
        layers = {}
        for layer in layers
            
        end
    end
    return pop

function mutation (population)
    assert(type(population) == "table", "Mutation expects a table")
    
    return population

--function crossover (pop)
    
--function selection (pop)

--TODO Tie to FCEUX
--TODO Read FCEUX docs
